/**
 * Organization Schema Component
 * 
 * DEPRECATED: Organization schema is now handled globally in app/layout.tsx
 * This component is kept for backwards compatibility but should not be used.
 * 
 * @component OrganizationSchema
 * @author Smarter Payouts Team
 * @deprecated Use global organizationSchema from @/src/lib/structured-data/schemas
 */
export default function OrganizationSchema() {
  // Organization schema is now handled globally in app/layout.tsx
  // This prevents duplicate Organization schemas
  return null;
}
